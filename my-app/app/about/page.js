import React from "react";

const About = () => {
  return <div>About page</div>;
};

export default About;

export const generateMetadata = () => {
  return {
    title: "About page",
    description: "This is all about page",
  };
};
